{
  "schema_version": "1.0",
  "record_type": "tool.launch_request",
  "description": "Arguments accepted by the Mermaid Editor launch entrypoint.",
  "type": "object",
  "properties": {
    "project_root": {
      "type": "string",
      "description": "Workspace project root used as runtime context."
    },
    "markdown_file": {
      "type": "string",
      "description": "Optional Markdown file opened on startup."
    }
  },
  "required": [
    "project_root"
  ],
  "additionalProperties": false
}
